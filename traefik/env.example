# Cloudflare API token — DNS edit permission on ignitesolutions.click only
# Cloudflare Dashboard → My Profile → API Tokens → Create Token
# Template: "Edit zone DNS" → scope to ignitesolutions.click
CF_DNS_API_TOKEN=
